name: Trigger auto deployment for container-app-xhytis7voxeg6

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-xhytis7voxeg6-AutoDeployTrigger-1e781bad-6331-4b24-a60b-7bb3f49fd134.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPXHYTIS7VOXEG6_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPXHYTIS7VOXEG6_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPXHYTIS7VOXEG6_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPXHYTIS7VOXEG6_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPXHYTIS7VOXEG6_REGISTRY_PASSWORD }}
          containerAppName: container-app-xhytis7voxeg6
          resourceGroup: todonodejspgsqlgoogleauth95909156
          imageToBuild: default/container-app-xhytis7voxeg6:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


